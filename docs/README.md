# Design documents

- `admin.md`: the technical design of this repository, including the migration from Create React App to Vite. Belongs here; edit it here.
- `DESIGN.md` and `contracts.md`: byte-identical copies of the originals in `wmsfo-api/docs`. Never edit them here; change the original and recopy.

Section references such as "contracts 4.5" point into `contracts.md`.
